# Alea hardening QA report — `df533f7` / follow-up `fix/alea-full-hardening`

## Scope

This report records the hardening change set prepared against commit `df533f7` and
the follow-up release gates executed on 2026-07-22. A check is marked complete only
when it has executable evidence.

## Implemented in this change set

| Area | Status | Evidence |
| --- | --- | --- |
| Roundtable participant policy | Implemented | API validation and database wrapper require exactly three distinct instances and at least two provider families. |
| Sporttery offer boundary | Implemented | Database eligibility predicate requires an authorized source, eligible competition, verified fact, `on_sale`, pre-cutoff state, matching source provenance, and an odds snapshot. |
| Empty autonomous selection | Implemented | The database returns `no_eligible_sporttery_matches`; it does not create an empty job. |
| Lifecycle bootstrap | Implemented through notarization | Lifecycle delivery calls an idempotent database initializer; the real local run completed `predict_score` → debate → vote → bet → notarize. |
| Worker executor configuration | Implemented as production factory | `ALEA_PHASE_EXECUTOR_FACTORY=app.workers.production_executor:create_phase_executor`; missing or invalid factories fail closed. |
| Unlicensed history ingestion | Blocked by policy | The backfill orchestrator requires all five authorization capabilities and contains no web scraper. |
| Supabase/JWT fallback | Removed | API and middleware no longer fall back to a hard-coded remote project URL; missing issuer configuration fails closed. |
| Consent versioning | Externalized | Signup refuses to create an account until active terms, privacy, and risk versions are configured. |
| Liveness/readiness | Separated | `/health` and `/livez` are process liveness; `/readyz` checks database, migration, Redis, Supabase, Dispatcher, Worker and executor factory. |
| CI and repository hygiene | Added | GitHub Actions, generated-artifact checks, conservative cleanup report, and ignore rules are included. |

## Local fix-pack and release validation

- Python compilation: passed.
- Focused unit and contract suite: 10 tests passed.
- GitHub Actions workflow YAML parse: passed.
- `bunx supabase db reset --yes`: passed from an empty local database, including migration `20260722010000`.
- `bunx supabase migration list --local`: passed; all local migrations are ordered through `20260722010000`.
- API: Ruff check, Ruff format check, Mypy and Pytest passed (`243 passed, 13 skipped, 1 warning`).
- Web: lint exit 0 (22 existing image optimization warnings), typecheck passed, Bun tests passed (`36 passed, 90 assertions`), production build passed.
- `/readyz`: HTTP 200 with database, migration, Redis, Supabase, Dispatcher, Worker and production executor checks true.
- Real local provider chain: job `74c3ca2c-8250-4836-aecf-e0b33650af92` completed with all 3 instances across DeepSeek API and Codex CLI; each persisted phase succeeded through `roundtable.notarized` and `roundtable.published_pending`.
- Real authenticated Playwright E2E: `ALEA_E2E_REAL=1 ALEA_E2E_STORAGE_STATE=/tmp/alea-e2e-auth.json PLAYWRIGHT_BASE_URL=http://127.0.0.1:3000 make test-e2e` passed 20/20 across desktop `1440×900` and mobile `390×844`. The test state used a real local Supabase account and did not set `ALEA_DEMO_ROLE`.
- In-app Browser visual verification was completed for `/console/admin/lineup` at desktop and mobile sizes. The API/CLI tabs, provider and CLI catalogs, disabled/unavailable states, responsive layout, and real backend-loaded directory state were inspected. Evidence: `/tmp/alea-iab-lineup-desktop.png`, `/tmp/alea-iab-lineup-cli-desktop.png`, `/tmp/alea-iab-lineup-api-mobile.png`, `/tmp/alea-iab-lineup-cli-mobile.png`.
- Docker full-stack validation passed with `docker compose -f docker-compose.yml build --progress=plain api worker worker-short dispatcher scheduler web`, followed by `docker compose -f docker-compose.yml up -d`; API, Web, Redis, Dispatcher, both Workers, Scheduler and Nginx were running, and container `/readyz` returned HTTP 200 with every readiness check true.

## Remaining release gates

- No licensed historical dataset is bundled and no claim is made that World Cup or
  five-league history has already been imported. The adapter, authorization evidence,
  batch execution, reconciliation, and coverage report must be supplied in the target
  environment.
- The in-app Browser evidence covers the provider lineup route only; a complete
  all-route visual regression pass remains outside this release-gate run.
- Marketing-only demonstration content remains explicitly labeled in the public
  marketing route. Production console routes now show database-backed content or an
  honest empty/unavailable state.

## Required release commands

```bash
python scripts/verify_hardening_contracts.py
make test-hardening
supabase db reset
make check
```

Record command, exit code, pass/fail/skip counts, commit SHA, and CI run URL. A task in
`2026-07-19-alea-full-implementation.md` may be changed to `[x]` only after its own
acceptance evidence is linked.
